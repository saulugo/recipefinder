<h1>Looking for Recipes with the ingredient <%= @search_term %></h1>

<% if @recipes.length == 0 %>

<p>Sorry, I didn't find recipes with the ingredient <%= @search_term %></p>

<% else %>

<table border="1">
	<tr>
		<th>Image</th>
		<th>Title</th>
		<th>Social Rank</th>
	</tr>
	<% @recipes.each do |recipe| %>
	<tr>
		<td><%= link_to image_tag(recipe["image_url"], size: "100"), recipe["source_url"] %></td>
		<td><%= sanitize(link_to recipe["title"], recipe["source_url"]) %></td>
		<td><%= recipe["social_rank"] %></td>
	</tr>
	<% end %>
</table>

<% end %>